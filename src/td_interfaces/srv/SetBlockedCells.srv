# Объявить набор клеток запрещёнными для ровера. Обрабатывается
# td_rover_executor/keepout_publisher, который перерисовывает маску keepout-фильтра
# глобального костмапа Nav2. Планировщик после этого сам строит объезд.

Cell[] cells
bool inflate_neighbours   # заблокировать и соседние клетки (запас на задержку данных)
---
bool success
string message
